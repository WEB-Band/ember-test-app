import Route from '@ember/routing/route';

// let isContact = true;

export default Route.extend({
    // con: this,
    model() {
        // if this.routeName == 'contact' {
            // return this.routeName;
        // }
    }
});
